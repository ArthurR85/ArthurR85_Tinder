<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Profil - Tinder von Arthur</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <style>
    
    .navbar-custom {
      background-color: #f8f9fa;
    }
    .brand-row {
      padding: 10px 0;
      text-align: center;
    }
    .nav-row {
      padding: 0;
    }
    .nav-row .nav {
      justify-content: center;
    }
    .profile-card {
      max-width: 400px;
      margin: 100px auto 0; 
      padding: 20px;
      border: 1px solid #ddd;
      border-radius: 10px;
      text-align: center;
      background-color: #fff;
    }
    .image-container {
      width: 100%;
      height: 300px;
      overflow: hidden;
      border-radius: 10px;
      margin-bottom: 15px;
    }
    .image-container img {
      width: 100%;
      object-fit: cover;
    }
  </style>
</head>
<body>
  
  <nav class="navbar navbar-custom fixed-top flex-column">
    <div class="brand-row">
      <a class="navbar-brand" href="/">Tinder von Arthur</a>
    </div>
    <div class="nav-row">
      <ul class="nav">
        <li class="nav-item"><a class="nav-link" href="/profil">Profil</a></li>
        <li class="nav-item"><a class="nav-link" href="/people">People</a></li>
        <li class="nav-item"><a class="nav-link" href="/filter">Filter</a></li>
        <li class="nav-item"><a class="nav-link" href="/matches">Matches</a></li>
      </ul>
    </div>
  </nav>

  
  <div class="container">
    <div class="profile-card">
      <div class="image-container">
        
        <img id="profileImage" src="<%= user && user.image_url ? user.image_url : '/default.jpg' %>" alt="Profilbild">
      </div>
      <div class="info">
        <p><strong>Name:</strong> <span id="profileName"><%= user ? user.name : 'Kein Profil' %></span></p>
        <p><strong>Geburtstag:</strong> <span id="profileBirthday"><%= user ? user.birthday : '-' %></span></p>
        <p><strong>Geschlecht:</strong> <span id="profileGender"><%= user ? user.gender : '-' %></span></p>
      </div>
      
      <button class="btn btn-primary" onclick="editProfile()">Profil bearbeiten</button>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    function editProfile() {
     
      alert("Bearbeitungsfunktion noch nicht implementiert!");
    }
  </script>
</body>
</html>
